#!/bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd $DIR/src
pip install gunicorn
pip install django
echo "Trying to start web service on port 8000."
gunicorn --workers=1 -b :8000 nnws.wsgi